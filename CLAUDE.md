# CLAUDE.md — 신랑신부 시식 예약 관리 서비스

## 프로젝트 개요
- 예식전 시식 예약 관리 서비스 (고객 페이지 + 관리자 화면)
- 파일 구성: index.html(고객), admin.html(관리자) — 각각 단일 파일

## 기술 규칙 (항상 지킬 것)
- 프레임워크 사용 금지 (순수 HTML/CSS/JS)
- 페이지는 index.html(고객) / admin.html(관리자) 2개. 공통 설정(config.js)과
  스타일(style.css)은 별도 파일로 두 페이지가 공유한다
  (Supabase 설정 중복을 피하기 위함)
- Supabase JS 라이브러리는 CDN 방식으로 불러온다
- 테이블: reservations (name, contact, hope_date, request, status[기본값 "대기"], created_at)
- service_role key와 비밀번호는 절대 코드에 넣지 않는다

## 범위 규칙 (절대 만들지 말 것)
- 고객 회원가입 X / 관리자 가입 기능 X / 결제 X

## 브랜드 규칙
- 톤: [따뜻하고 아늑한] / 메인 컬러: [#b98a5e]
- 말투: 존댓말, 버튼 문구는 행동이 드러나게 ("예약 문의하기")

## 코드 스타일
- 주석은 한국어로, 비전공자가 읽을 수 있게
- 함수와 변수 이름은 의미가 드러나게

## 대화 규칙
- 파일을 수정할 때는 먼저 무엇을 왜 바꾸는지 한 줄로 설명하고 diff를 보여준다
- 파일을 새로 만들 때는 어디에 어떤 이름으로 만드는지 알려준다
