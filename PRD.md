# PRD — 웨딩홀 신랑신부 시식 예약 관리 서비스

## 한 줄 소개
주말 예식 전 시식 일정을 소개하고, 손님이 예약 문의를 남기면
사장이 관리자 화면에서 접수 내역을 확인·확정·삭제할 수 있는 서비스.

## 핵심 기능 (5가지 — 이 범위를 넘지 않는다)
1. **서비스 소개**: 클래스 설명, 가격, 위치를 보여준다
2. **예약 문의 접수 (Create)**: 이름·연락처·희망 날짜·요청사항을 입력받아 저장한다
3. **관리자 조회 (Read)**: 사장만 로그인해서 접수 내역을 최신순으로 본다
4. **접수 상태 변경 (Update)**: 사장이 "대기 → 확정"으로 상태를 바꾼다
5. **접수 삭제 (Delete)**: 사장이 스팸·취소 접수를 삭제한다

## 화면 구성
### 고객 페이지 (index.html)
- 상단: 대표 문구 + 서비스 한 줄 소개 (히어로)
- 중간: 소개 카드 3개 (무엇을 / 얼마에 / 어디서)
- 하단: 예약 문의 폼 (이름, 연락처, 희망 날짜, 요청사항, 접수 버튼)

### 관리자 화면 (admin.html)
- 로그인 폼 (이메일, 비밀번호)
- 로그인 후: 접수 내역 표 + 각 행에 [확정] [삭제] 버튼
- 상태별 표시: 대기(회색) / 확정(초록)
- 로그아웃 버튼

## 데이터 모델 (reservations)
| 필드 | 타입 | 비고 |
|------|------|------|
| id | bigint (자동) | PK |
| name | text | 이름 (필수) |
| contact | text | 연락처 (필수, **개인정보**) |
| hope_date | date | 희망 날짜 |
| request | text | 요청사항 |
| status | text | 상태, **기본값 "대기"** · 허용값: `대기` / `확정` |
| created_at | timestamptz (자동) | 접수 시각 · **조회 정렬 기준(최신순)** |

## 권한 설계 (누가 무엇을)
| 역할 | 로그인 | 가능한 동작 |
|------|--------|-------------|
| 손님 | 없음 | 접수 남기기(Create) **만** |
| 사장 | 함 | 조회(Read) · 상태 변경(Update) · 삭제(Delete) |

### ⚠️ 권한 구현 시 반드시 지켜야 할 보안 조건
명세의 권한 설계는 옳지만, Supabase 구현에서 아래를 지키지 않으면 손님도
관리자 권한을 얻을 수 있다. **아래 3가지는 기능이 아니라 필수 설정이다.**

1. **신규 회원가입 차단 (필수)**
   Supabase는 기본적으로 누구나 `auth.signUp()`으로 가입해 `authenticated`
   역할을 얻을 수 있다. 화면에 가입 버튼이 없어도 API로 우회 가입이 가능하다.
   → Authentication > Sign In / Providers 에서 **"Allow new users to sign up" 끄기**.
   관리자 계정 1개는 Supabase 대시보드에서 직접 생성한다.

2. **RLS(행 단위 보안) 활성화 (필수)**
   공개 anon key의 안전성은 전적으로 RLS에 의존한다. RLS를 끄면 anon key로
   누구나 전체 데이터를 읽고 지울 수 있다. → 테이블에 `enable row level security` 필수.

3. **손님 insert 시 status 고정**
   손님이 접수하며 `status:'확정'`을 임의로 넣어 스스로 확정하지 못하도록,
   anon insert 정책은 `with check (status = '대기')`로 제한한다.

### 권한 정책 (RLS) 요약
- `anon`: INSERT 만 허용 (`with check (status = '대기')`), SELECT/UPDATE/DELETE 불가
- `authenticated`: SELECT / UPDATE / DELETE 허용
- 결과: 손님은 남의 접수를 볼 수 없고, 연락처 등 개인정보는 로그인한 사장만 열람 가능

## UX 규칙 (구현 기준 명문화)
- 접수 성공/실패 시 폼 하단에 안내 메시지 표시, 성공 시 폼 초기화
- 관리자 조회 결과가 없으면 "아직 접수된 문의가 없습니다" 빈 상태 표시
- 확정된 접수는 [확정] 버튼을 숨기고 초록 배지 표시
- 삭제는 확인창(confirm)으로 오조작 방지

## 비기능 요건
- 정적 호스팅 (로컬 파일 / GitHub Pages / Netlify 등), 별도 서버 없음
- 백엔드·인증은 Supabase 사용
- 관리자 계정은 1개 전제

## 범위 제외 (만들지 않는 것)
- 고객 회원가입 없음 (손님은 로그인 없이 접수, 변경은 사장에게 연락)
- 관리자 "가입" 기능 없음 (계정 1개는 Supabase 화면에서 직접 생성)
- 결제 기능 없음
